# BMad Method (BMM) module configuration
# Used by workflows such as bmad-create-prd (main_config in workflow.md)
# Version: aligned with BMAD installer 6.2.x patterns

project_name: shopify-letters
user_name: Eric

communication_language: English
document_output_language: English
user_skill_level: intermediate

# Root for generated artifacts (brainstorming, exports, etc.)
output_folder: "{project-root}/_bmad-output"

# Planning-phase docs (PRD, briefs, etc.) — explicit path so outputFile resolves without nested vars
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"

# Optional: repo docs the workflow searches for brownfield / context (create folder when you add docs)
project_knowledge: "{project-root}/docs"
